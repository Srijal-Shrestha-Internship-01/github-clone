import React from "react";

export const ToInput = ()=> {
      
    return (
        <div>
        <input type="text" onChange ={this.handle.bind(this)}        />
        </div>
    );
};
            